<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width,initial-scale=1.0">
  <link href="/css/output.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Fredoka+One&display=swap" rel="stylesheet">
  <title>Edit Course #<%= values.id %></title>
</head>
<body class="bg-primary text-secondary font-cartoon dotted-bg">
  <header class="fixed z-10 w-full shadow bg-primary comic-border">
    <div class="container flex items-center justify-between px-4 py-6 mx-auto">
      <h1 class="text-4xl font-bold text-secondary">
        <a href="/" class="text-secondary">Courses</a>
      </h1>
      <div>
        <% if (user) { %>
          <a href="/profile" class="text-secondary">
            <img src="/path/to/profile-icon.png" alt="Profile" class="w-8 h-8 rounded-full">
          </a>
        <% } else { %>
          <a href="/login" class="mr-4 text-secondary">Login</a>
          <a href="/register" class="text-secondary">Register</a>
        <% } %>
      </div>
    </div>
  </header>

  <main class="container px-4 py-8 pt-32 mx-auto">
    <div class="max-w-lg p-6 mx-auto rounded-lg shadow-lg bg-primary comic-border">
      <h2 class="mb-6 text-3xl font-bold text-center text-secondary">Edit Course #<%= values.id %></h2>

      <% if (error) { %>
        <div class="px-4 py-3 mb-4 text-red-700 bg-red-100 border border-red-400 rounded">
          <%= error %>
        </div>
      <% } %>

      <form action="/courses/<%= values.id %>/edit" method="POST">
        <div class="mb-4">
          <label for="title" class="block mb-2 font-bold text-secondary">Title</label>
          <input
            type="text"
            id="title"
            name="title"
            class="w-full px-3 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-accent"
            value="<%= values.title %>"
            required
          >
        </div>

        <div class="mb-6">
          <label for="description" class="block mb-2 font-bold text-secondary">Description</label>
          <textarea
            id="description"
            name="description"
            rows="5"
            class="w-full px-3 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-accent"
            required
          ><%= values.description %></textarea>
        </div>

        <div class="flex items-center justify-between">
          <button
            type="submit"
            class="px-4 py-2 text-white rounded-lg bg-accent hover:bg-accent-dark focus:outline-none focus:ring-2 focus:ring-accent"
          >
            Update Course
          </button>
          <a href="/courses/<%= values.id %>" class="font-bold text-secondary hover:text-accent">Cancel</a>
        </div>
      </form>
    </div>
  </main>

  <footer class="mt-8 shadow bg-primary comic-border">
    <div class="container px-4 py-4 mx-auto text-center">
      <p class="text-secondary">&copy; 2025 Courses Page. All rights reserved.</p>
    </div>
  </footer>
</body>
</html>